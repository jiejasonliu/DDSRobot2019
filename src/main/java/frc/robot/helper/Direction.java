package frc.robot.helper;

/**
 * Enum that helps with the rotation of seeker or locator. The logic changes depending on the enum value.
 * 
 * @see LocateTargetCommand
 */
public enum Direction {
    CLOCKWISE, COUNTERCLOCKWISE;
}